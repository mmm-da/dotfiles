include ./common-vars

input type:touchpad {
    tap enabled
    natural_scroll true
    dwt enabled
    accel_profile "flat" 
    pointer_accel 1
}

input type:mouse {
    accel_profile "flat" 
    pointer_accel 0.5 
}
